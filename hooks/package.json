{
  "name": "design-space-hooks",
  "version": "2.0.0",
  "type": "module",
  "description": "Design Space hooks and The Conductor — agent session manager",
  "scripts": {
    "conductor": "node conductor.js",
    "conductor:auto": "node conductor.js --auto-launch",
    "conductor:stockholm": "node conductor.js --auto-launch --machine stockholm",
    "conductor:laptop": "node conductor.js --auto-launch --machine laptop",
    "listen": "node orchestrator.js",
    "listen:auto": "node orchestrator.js --auto-launch"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0",
    "node-telegram-bot-api": "^0.66.0"
  }
}
